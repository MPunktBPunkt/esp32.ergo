# debug/calib/

Kennflächen-Snapshots. `powermap-latest.json` zeigt auf die datierte Datei
(`powermap-20260912T124351Z.json`). Kanonische Map für Doku:
`docs/ergometer/kalibrierung-map-20260915.json` (Watt-Raster aus der Heatmap)
bzw. `docs/ergometer/kalibrierung-map-20260913.json` (letzter API-Dump mit
`n`/`s`/`t`). Siehe `KALIBRIERUNG.md`.
