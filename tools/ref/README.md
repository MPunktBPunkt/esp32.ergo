# Referenz-Decoder der Sonde (esp32.ftmsprobe).
#
# Unveraenderte Kopie von `nodes/esp32.ftmsprobe/tools/ftms.py`, damit
# `make-fixtures.py --verify-curated` und CI ohne das Sonden-Repo laufen.
# Sollwerte fuer Codec-Fixtures kommen aus DIESEM Modul, nicht aus dem C++-Codec.
